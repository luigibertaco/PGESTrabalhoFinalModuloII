<p id="notice"><%= notice %></p>

<p>
  <strong>Username:</strong>
  <%= @usuario.username %>
</p>

<p>
  <strong>Ativo:</strong>
  <%= @usuario.ativo ? 'Ativo' : 'Inativo' %>
</p>

<p>
  <strong>Setor:</strong>
  <%= @usuario.setor ? @usuario.setor.descricao : '--' %>
</p>

<%= link_to 'Edit', edit_usuario_path(@usuario) %> |
<%= link_to 'Back', usuarios_path %>
